{
  "$schema": "http://json-schema.org/schema",
  "$id": "eensy-cli.generate:validators",
  "title": "Generate resource validators",
  "type": "object",
  "description": "Generates the base validators for a resource inside an eensy project",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the resource",
      "x-prompt": "Enter the name of the resource"
    },
    "service": {
      "type": "string",
      "description": "The name of the service that will contain the resource validators",
      "x-prompt": "Enter the name of the service that will contain the resource validators"
    }
  },
  "required": ["name", "service"]
}
